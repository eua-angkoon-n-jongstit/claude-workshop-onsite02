services:
  test-db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: booking_test
    ports:
      - "5433:5432"
    tmpfs:
      - /var/lib/postgresql/data # throwaway — no persistence needed between runs
